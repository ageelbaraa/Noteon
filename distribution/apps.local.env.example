# Optional local overrides for distribution scripts (gitignored).
# Copy from apps.env and adjust groups, notes, or CI tokens as needed.
#
# FIREBASE_TESTER_GROUPS=qa,internal
# FIREBASE_RELEASE_NOTES=Build from CI
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json
